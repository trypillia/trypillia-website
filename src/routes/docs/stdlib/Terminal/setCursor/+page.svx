<script lang="ts">
	import CodeBlock from '$lib/components/CodeBlock.svelte';
	import MethodBreadcrumb from '$lib/components/MethodBreadcrumb.svelte';

	const example = `Terminal.setCursor(1, 1);`;
</script>

<svelte:head>
	<title>Terminal.setCursor — Trypillia</title>
</svelte:head>

<MethodBreadcrumb module="Terminal" title="Terminal" name="setCursor" />

<section>

## Terminal.setCursor

<CodeBlock code={`Terminal.setCursor(x: Int, y: Int) -> Void`} />

Переміщує курсор у задану позицію (стовпець `x`, рядок `y`).

</section>

<section>

### Параметри

| Параметр | Опис |
| --- | --- |
| `x: Int` | Номер стовпця. |
| `y: Int` | Номер рядка. |

</section>

<section>

### Повертає

`Void` — нічого не повертає.

</section>

<section>

### Приклад

<CodeBlock code={example} />

</section>
