<script lang="ts">
	import CodeBlock from '$lib/components/CodeBlock.svelte';
	import MethodBreadcrumb from '$lib/components/MethodBreadcrumb.svelte';

	const example = `assertThrows(fn() { OS.getEnv("НЕМА").unwrap(); });`;
</script>

<svelte:head>
	<title>assertThrows — Trypillia</title>
</svelte:head>

<MethodBreadcrumb module="Test" title="Test" name="assertThrows" />

<section>

## assertThrows

<CodeBlock code={`assertThrows(fn: Function, message: String) -> Void`} />

Перевіряє, що передана функція кидає помилку під час виконання.

</section>

<section>

### Параметри

| Параметр | Опис |
| --- | --- |
| `fn: Function` | Функція, яка має кинути помилку. |
| `message: String` | Повідомлення (необовʼязково). |

</section>

<section>

### Повертає

`Void` — нічого не повертає.

</section>

<section>

### Приклад

<CodeBlock code={example} />

</section>
