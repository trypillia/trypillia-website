<script lang="ts">
	import CodeBlock from '$lib/components/CodeBlock.svelte';
	import MethodBreadcrumb from '$lib/components/MethodBreadcrumb.svelte';

	const example = `beforeEach(fn() { print("перед тестом"); });`;
</script>

<svelte:head>
	<title>beforeEach — Trypillia</title>
</svelte:head>

<MethodBreadcrumb module="Test" title="Test" name="beforeEach" />

<section>

## beforeEach

<CodeBlock code={`beforeEach(fn: Function) -> Void`} />

Реєструє функцію, яка виконується перед кожним тестом.

</section>

<section>

### Параметри

| Параметр | Опис |
| --- | --- |
| `fn: Function` | Функція підготовки. |

</section>

<section>

### Повертає

`Void` — нічого не повертає.

</section>

<section>

### Приклад

<CodeBlock code={example} />

</section>
