<script lang="ts">
	import CodeBlock from '$lib/components/CodeBlock.svelte';
	import MethodBreadcrumb from '$lib/components/MethodBreadcrumb.svelte';

	const example = `xit("поки не реалізовано");`;
</script>

<svelte:head>
	<title>xit — Trypillia</title>
</svelte:head>

<MethodBreadcrumb module="Test" title="Test" name="xit" />

<section>

## xit

<CodeBlock code={`xit(name: String) -> Void`} />

Пропускає тест-кейс (позначає його як пропущений).

</section>

<section>

### Параметри

| Параметр | Опис |
| --- | --- |
| `name: String` | Назва пропущеного тесту. |

</section>

<section>

### Повертає

`Void` — нічого не повертає.

</section>

<section>

### Приклад

<CodeBlock code={example} />

</section>
