<script lang="ts">
	import CodeBlock from '$lib/components/CodeBlock.svelte';
	import MethodBreadcrumb from '$lib/components/MethodBreadcrumb.svelte';

	const example = `it("працює", fn() { assert(true); });`;
</script>

<svelte:head>
	<title>it — Trypillia</title>
</svelte:head>

<MethodBreadcrumb module="Test" title="Test" name="it" />

<section>

## it

<CodeBlock code={`it(name: String, fn: Function) -> Void`} />

Оголошує окремий тест-кейс.

</section>

<section>

### Параметри

| Параметр | Опис |
| --- | --- |
| `name: String` | Назва тесту. |
| `fn: Function` | Тіло тесту. |

</section>

<section>

### Повертає

`Void` — нічого не повертає.

</section>

<section>

### Приклад

<CodeBlock code={example} />

</section>
