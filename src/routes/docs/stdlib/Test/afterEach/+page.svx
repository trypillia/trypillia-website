<script lang="ts">
	import CodeBlock from '$lib/components/CodeBlock.svelte';
	import MethodBreadcrumb from '$lib/components/MethodBreadcrumb.svelte';

	const example = `afterEach(fn() { print("після тесту"); });`;
</script>

<svelte:head>
	<title>afterEach — Trypillia</title>
</svelte:head>

<MethodBreadcrumb module="Test" title="Test" name="afterEach" />

<section>

## afterEach

<CodeBlock code={`afterEach(fn: Function) -> Void`} />

Реєструє функцію, яка виконується після кожного тесту.

</section>

<section>

### Параметри

| Параметр | Опис |
| --- | --- |
| `fn: Function` | Функція очищення. |

</section>

<section>

### Повертає

`Void` — нічого не повертає.

</section>

<section>

### Приклад

<CodeBlock code={example} />

</section>
