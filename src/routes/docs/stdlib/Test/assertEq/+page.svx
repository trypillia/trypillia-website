<script lang="ts">
	import CodeBlock from '$lib/components/CodeBlock.svelte';
	import MethodBreadcrumb from '$lib/components/MethodBreadcrumb.svelte';

	const example = `assertEq("hello".length(), 5);`;
</script>

<svelte:head>
	<title>assertEq — Trypillia</title>
</svelte:head>

<MethodBreadcrumb module="Test" title="Test" name="assertEq" />

<section>

## assertEq

<CodeBlock code={`assertEq(actual: Any, expected: Any, message: String) -> Void`} />

Перевіряє рівність двох значень.

</section>

<section>

### Параметри

| Параметр | Опис |
| --- | --- |
| `actual: Any` | Фактичне значення. |
| `expected: Any` | Очікуване значення. |
| `message: String` | Повідомлення (необовʼязково). |

</section>

<section>

### Повертає

`Void` — нічого не повертає.

</section>

<section>

### Приклад

<CodeBlock code={example} />

</section>
