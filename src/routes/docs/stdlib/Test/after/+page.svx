<script lang="ts">
	import CodeBlock from '$lib/components/CodeBlock.svelte';
	import MethodBreadcrumb from '$lib/components/MethodBreadcrumb.svelte';

	const example = `after(fn() { print("кінець"); });`;
</script>

<svelte:head>
	<title>after — Trypillia</title>
</svelte:head>

<MethodBreadcrumb module="Test" title="Test" name="after" />

<section>

## after

<CodeBlock code={`after(fn: Function) -> Void`} />

Реєструє функцію, яка виконується один раз після всіх тестів.

</section>

<section>

### Параметри

| Параметр | Опис |
| --- | --- |
| `fn: Function` | Функція завершення. |

</section>

<section>

### Повертає

`Void` — нічого не повертає.

</section>

<section>

### Приклад

<CodeBlock code={example} />

</section>
