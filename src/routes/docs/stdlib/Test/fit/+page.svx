<script lang="ts">
	import CodeBlock from '$lib/components/CodeBlock.svelte';
	import MethodBreadcrumb from '$lib/components/MethodBreadcrumb.svelte';

	const example = `fit("лише цей", fn() { assert(true); });`;
</script>

<svelte:head>
	<title>fit — Trypillia</title>
</svelte:head>

<MethodBreadcrumb module="Test" title="Test" name="fit" />

<section>

## fit

<CodeBlock code={`fit(name: String, fn: Function) -> Void`} />

Фокусує окремий тест-кейс: виконуються лише сфокусовані тести.

</section>

<section>

### Параметри

| Параметр | Опис |
| --- | --- |
| `name: String` | Назва тесту. |
| `fn: Function` | Тіло тесту. |

</section>

<section>

### Повертає

`Void` — нічого не повертає.

</section>

<section>

### Приклад

<CodeBlock code={example} />

</section>
