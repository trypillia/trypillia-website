<script lang="ts">
	import CodeBlock from '$lib/components/CodeBlock.svelte';
	import MethodBreadcrumb from '$lib/components/MethodBreadcrumb.svelte';

	const example = `before(fn() { print("старт"); });`;
</script>

<svelte:head>
	<title>before — Trypillia</title>
</svelte:head>

<MethodBreadcrumb module="Test" title="Test" name="before" />

<section>

## before

<CodeBlock code={`before(fn: Function) -> Void`} />

Реєструє функцію, яка виконується один раз перед усіма тестами.

</section>

<section>

### Параметри

| Параметр | Опис |
| --- | --- |
| `fn: Function` | Функція підготовки. |

</section>

<section>

### Повертає

`Void` — нічого не повертає.

</section>

<section>

### Приклад

<CodeBlock code={example} />

</section>
