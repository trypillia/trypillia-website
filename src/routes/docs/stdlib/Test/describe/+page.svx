<script lang="ts">
	import CodeBlock from '$lib/components/CodeBlock.svelte';
	import MethodBreadcrumb from '$lib/components/MethodBreadcrumb.svelte';

	const example = `describe("математика", fn() {
  it("додавання", fn() { assertEq(1 + 1, 2); });
});`;
</script>

<svelte:head>
	<title>describe — Trypillia</title>
</svelte:head>

<MethodBreadcrumb module="Test" title="Test" name="describe" />

<section>

## describe

<CodeBlock code={`describe(name: String, fn: Function) -> Void`} />

Групує повʼязані тести під спільною назвою.

</section>

<section>

### Параметри

| Параметр | Опис |
| --- | --- |
| `name: String` | Назва групи. |
| `fn: Function` | Функція з тестами. |

</section>

<section>

### Повертає

`Void` — нічого не повертає.

</section>

<section>

### Приклад

<CodeBlock code={example} />

</section>
