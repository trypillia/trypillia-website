<script lang="ts">
	import CodeBlock from '$lib/components/CodeBlock.svelte';
	import MethodBreadcrumb from '$lib/components/MethodBreadcrumb.svelte';

	const example = `fdescribe("лише ця група", fn() {
  it("тест", fn() { assert(true); });
});`;
</script>

<svelte:head>
	<title>fdescribe — Trypillia</title>
</svelte:head>

<MethodBreadcrumb module="Test" title="Test" name="fdescribe" />

<section>

## fdescribe

<CodeBlock code={`fdescribe(name: String, fn: Function) -> Void`} />

Фокусує групу тестів: виконуються лише сфокусовані групи.

</section>

<section>

### Параметри

| Параметр | Опис |
| --- | --- |
| `name: String` | Назва групи. |
| `fn: Function` | Функція з тестами. |

</section>

<section>

### Повертає

`Void` — нічого не повертає.

</section>

<section>

### Приклад

<CodeBlock code={example} />

</section>
