<script lang="ts">
	import CodeBlock from '$lib/components/CodeBlock.svelte';
	import MethodBreadcrumb from '$lib/components/MethodBreadcrumb.svelte';

	const example = `assert(1 + 1 == 2, "математика працює");`;
</script>

<svelte:head>
	<title>assert — Trypillia</title>
</svelte:head>

<MethodBreadcrumb module="Test" title="Test" name="assert" />

<section>

## assert

<CodeBlock code={`assert(condition: Any, message: String) -> Void`} />

Перевіряє, що умова істинна. Якщо ні — тест провалюється з необовʼязковим повідомленням.

</section>

<section>

### Параметри

| Параметр | Опис |
| --- | --- |
| `condition: Any` | Умова, що має бути істинною. |
| `message: String` | Повідомлення про помилку (необовʼязково). |

</section>

<section>

### Повертає

`Void` — нічого не повертає.

</section>

<section>

### Приклад

<CodeBlock code={example} />

</section>
