<script lang="ts">
	import CodeBlock from '$lib/components/CodeBlock.svelte';
	import MethodBreadcrumb from '$lib/components/MethodBreadcrumb.svelte';

	const example = `assertNeq("a", "b");`;
</script>

<svelte:head>
	<title>assertNeq — Trypillia</title>
</svelte:head>

<MethodBreadcrumb module="Test" title="Test" name="assertNeq" />

<section>

## assertNeq

<CodeBlock code={`assertNeq(actual: Any, unexpected: Any, message: String) -> Void`} />

Перевіряє, що два значення НЕ рівні.

</section>

<section>

### Параметри

| Параметр | Опис |
| --- | --- |
| `actual: Any` | Фактичне значення. |
| `unexpected: Any` | Значення, якому воно не має дорівнювати. |
| `message: String` | Повідомлення (необовʼязково). |

</section>

<section>

### Повертає

`Void` — нічого не повертає.

</section>

<section>

### Приклад

<CodeBlock code={example} />

</section>
