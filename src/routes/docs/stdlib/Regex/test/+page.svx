<script lang="ts">
	import CodeBlock from '$lib/components/CodeBlock.svelte';
	import MethodBreadcrumb from '$lib/components/MethodBreadcrumb.svelte';

	const example = `print(Regex.test("[0-9]+", "abc123"));  // true`;
</script>

<svelte:head>
	<title>Regex.test — Trypillia</title>
</svelte:head>

<MethodBreadcrumb module="Regex" title="Regex" name="test" />

<section>

## Regex.test

<CodeBlock code={`Regex.test(pattern: String, text: String) -> Bool`} />

Перевіряє, чи відповідає текст заданому регулярному виразу. Повертає `nil`, якщо шаблон некоректний.

</section>

<section>

### Параметри

| Параметр | Опис |
| --- | --- |
| `pattern: String` | Регулярний вираз. |
| `text: String` | Текст для перевірки. |

</section>

<section>

### Повертає

`Bool` — чи знайдено відповідність.

</section>

<section>

### Приклад

<CodeBlock code={example} />

</section>
